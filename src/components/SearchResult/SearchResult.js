import React, { Component } from "react";

/**
 * Result box to show in both shop search result and user search result
 * @param: info = {value}
 * - name
 * - category
 * - city, prov
 * - logo
 * - premiums left
 * - goalsDone
 * - price
 * - priceIncrease
 */
export class SearchResult extends Component {
  render() {
    return <div></div>;
  }
}

export default SearchResult;
